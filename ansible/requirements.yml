---
# Ansible Collections required for this project
# Install with: ansible-galaxy collection install -r requirements.yml
# NOTE: Using containers.podman from Red Hat's certified collection for AAP

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: containers.podman
    version: ">=1.13.0"
  - name: awx.awx
    version: ">=24.6.1"
  - name: dynatrace.oneagent
    version: ">=1.6.0"
  - name: ansible.eda
    version: ">=2.0.0"
  # podman_container, podman_image, and related modules
  # These are certified for Red Hat Ansible Automation Platform
